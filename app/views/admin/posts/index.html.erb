<div class="list-group">
  <% @posts.each do |post| %>
      <a href="/admin/posts/<%= post.friendly_id%>" class="list-group-item">
        <h4 class="list-group-item-heading"><%= post.title %></h4>
        <p class="list-group-item-text"><%= post.content %></p>
      </a>
  <% end %>
</div>

<%= link_to 'Create new Post', new_admin_post_path, class: 'btn brand-color-box'%>
